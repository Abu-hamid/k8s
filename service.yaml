kind: service
apiVersion: v1
metadata:
  name: nginx-service
  namespace: nginx
spec:
  selector:
    app: nginx
    ports:
      - protocol: TCP
        port: 80               # servicePort
        targetPort: 80         # containerPort
  type: ClusterIP